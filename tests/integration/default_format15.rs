// Test case that compares a file generated by rust_xlsxwriter with a file
// created by Excel.
//
// SPDX-License-Identifier: MIT OR Apache-2.0
//
// Copyright 2022-2025, John McNamara, jmcnamara@cpan.org

use crate::common;
use rust_xlsxwriter::{worksheet::Worksheet, Format, Url, Workbook, XlsxError};

// Test to demonstrate adding images to worksheets.
fn create_new_xlsx_file_1(filename: &str) -> Result<(), XlsxError> {
    let mut workbook = Workbook::new();

    // Set the default format for the workbook.
    let format = Format::new().set_font_name("Arial").set_font_size(10);
    workbook.set_default_format(&format, 17, 64)?;

    let worksheet = workbook.add_worksheet();

    // Write a url to test the default hyperlink style/format.
    worksheet.write(0, 0, Url::new("https://www.cpan.org/"))?;

    workbook.save(filename)?;

    Ok(())
}

// Test to demonstrate adding images to worksheets.
fn create_new_xlsx_file_2(filename: &str) -> Result<(), XlsxError> {
    let mut workbook = Workbook::new();

    // Set the default format for the workbook.
    let format = Format::new().set_font_name("Arial").set_font_size(10);
    workbook.set_default_format(&format, 17, 64)?;

    let mut worksheet = Worksheet::new();
    // Repeat setting the format for a standalone worksheet.
    worksheet.set_default_format(&format, 17, 64)?;

    // Write a url to test the default hyperlink style/format.
    worksheet.write(0, 0, Url::new("https://www.cpan.org/"))?;

    workbook.push_worksheet(worksheet);

    workbook.save(filename)?;

    Ok(())
}

#[test]
fn test_default_format15_1() {
    let test_runner = common::TestRunner::new()
        .set_name("default_format15")
        .set_function(create_new_xlsx_file_1)
        .unique("1")
        .initialize();

    test_runner.assert_eq();
    test_runner.cleanup();
}

#[test]
fn test_default_format15_2() {
    let test_runner = common::TestRunner::new()
        .set_name("default_format15")
        .set_function(create_new_xlsx_file_2)
        .unique("2")
        .initialize();

    test_runner.assert_eq();
    test_runner.cleanup();
}
