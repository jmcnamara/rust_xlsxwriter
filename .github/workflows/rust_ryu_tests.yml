name: Rust - test ryu feature

on:
  push:
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Run the tests for the ryu feature set
      run: cargo test --test integration --features ryu