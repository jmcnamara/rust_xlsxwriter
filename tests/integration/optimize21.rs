// Test case that compares a file generated by rust_xlsxwriter with a file
// created by Excel.
//
// SPDX-License-Identifier: MIT OR Apache-2.0
//
// Copyright 2022-2024, John McNamara, jmcnamara@cpan.org

use crate::common;
use rust_xlsxwriter::{Format, Workbook, XlsxError};

// Create rust_xlsxwriter file to compare against Excel file.
fn create_new_xlsx_file(filename: &str) -> Result<(), XlsxError> {
    let mut workbook = Workbook::new();
    let mut format = Format::new().set_bold().set_italic();

    // Register the formats.
    workbook.register_format(&mut format);

    // Constant memory worksheet.
    let worksheet = workbook.add_worksheet().set_constant_memory_mode(true)?;
    worksheet.write_number_with_format(0, 0, 123, &format)?;
    worksheet.set_portrait();

    // Standard in-memory worksheet.
    let worksheet = workbook.add_worksheet();
    worksheet.write_number_with_format(0, 0, 123, &format)?;
    worksheet.set_portrait();

    workbook.save(filename)?;

    Ok(())
}

#[test]
fn test_optimize21() {
    let test_runner = common::TestRunner::new()
        .set_name("optimize21")
        .set_function(create_new_xlsx_file)
        .initialize();

    test_runner.assert_eq();
    test_runner.cleanup();
}
